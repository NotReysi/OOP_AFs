{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar (g++)",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${fileDirname}/MinHeap.cpp",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compila el archivo C++ con g++",
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}
