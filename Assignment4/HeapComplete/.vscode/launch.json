{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ (Pinchecha's Debug)", // ¡El nombre que verás!
            "type": "cppdbg",
            "request": "launch",
            // Le dice que corra el .exe que acabamos de crear
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false, // ¡Corre en la terminal de abajo!
            "MIMode": "gdb",
            // ¡¡OJO AQUÍ TILÍN!! 👇
            // Esta ruta la adiviné de tus errores, ¡debería jalar!
            "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            
            // ¡¡LA LÍNEA MÁS IMPORTANTE!!
            // "Antes de 'lanzar' (run), corre esta tarea:"
            "preLaunchTask": "Build C++ (Pinchecha's Build)"
        }
    ]
}